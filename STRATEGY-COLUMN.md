# 博客栏目策略（通用模板）

> 最后更新：2026-05-14（新增"中国金融的故事"）
>
> 本文档记录在博客中创建和管理栏目的通用方法论。具体栏目策略见 `DEEP-LEARNING-COLUMN-STRATEGY.md` 等文件。

---

## 一、内容层级

文章通过四个维度组织，前三者为树状层级，标签为扁平交叉：

```
门类 (genre)          vibe / my                  # 最顶级，首页分两列入口
  └─ 栏目 (column)     栏目名                     # 连载容器，文末自动导航
       └─ 系列 (series) 系列名                    # 栏目内的子分组（可选）

标签 (tags)           [标签1, 标签2]             # 扁平，跨门类/栏目/系列
```

### Frontmatter 字段

```yaml
---
title: 文章标题
date: 2026-05-14
genre: vibe             # vibe / my，必填
column: 栏目名           # 可选，归属连载栏目。文末自动生成上下篇导航
series: 系列名           # 可选，栏目内子分组
order: 1                # 可选，column 内排序依据。不设则按 date 排
tags: [标签1, 标签2]    # 可选，扁平标签
description: 摘要
draft: true             # 草稿，不出现在首页和搜索中
pin: true               # 可选，置顶文章
---
```

新文章直接在 `content/posts/` 下创建 `.md` 文件即可，无需注册路由。

---

## 二、栏目规划

### 建立步骤

1. **确定定位**——这个栏目讲什么？目标读者是谁？内容范围？
2. **划定内容边界**——明确包含什么、不包含什么
3. **设计结构**——按时间线 / 主题 / 难度分级
4. **确定叙事风格**——第三人称叙事？技术教程？对话体？
5. **规划节点列表**——每篇文章覆盖什么内容，预估顺序
6. **建立规范**——图片、公式、引用等格式约定

### 排序规则

同一 column 内的文章通过 frontmatter 的 `order` 字段控制顺序（升序）。不设 `order` 的文章按 `date` 排，排在所有设了 `order` 的文章之后。

### 导航

依赖 React 组件（`Post.tsx`）按上述排序自动生成上一篇/下一篇，无需在 markdown 中手动添加链接。

---

## 三、命名约定

文件名直接用内容关键词，不加栏目名前缀。

```
好的例子：
  感知机的诞生.md          # 深度学历史栏目·感知机篇
  从零实现反向传播.md      # 技术笔记·BP 实现

不好的例子：
  深度学习的故事-感知机的诞生.md
```

标题直接用主题当标题，不加栏目名前缀。

---

## 四、图像与格式

### 图片

- 图片放在 `public/images/` 目录下
- markdown 中使用 `![](/images/文件名.jpg)` 引用
- 如需人物肖像，可在文末设独立章节集中展示
- 人物肖像姓名后用 `<strong>` 而非 `**` 加粗（因 `<img>` 后的 markdown 语法会被解析器忽略）

### 图片选材原则

**不用大 Logo、不用产品截图、不用公关宣传图。** 图片的作用是把读者拉回那个年代。

选材优先级：

1. **团队早期合影 / 工作场景**——最有温度。几个人挤在工位前的照片 > 舞台上的发布会
2. **关键场景纪实**——用户在使用产品、团队在熬夜上线、街头巷尾的时代切片
3. **历史性瞬间**——有故事感的单帧画面，能让人追问"然后呢？"
4. **人物肖像**——尽量选成名前的照片，而非功成名就后的公关照

图注写清楚：**拍摄时间 + 地点 + 画面背景**，不只是人名/物名。

具体栏目的图片指引见各自策略文件。

### 公式

```
行内：$E = mc^2$
展示：$$E = mc^2$$
```

不支持 `\[` / `\(` 语法（remark-math 限制）。

### 代码高亮

使用 markdown 标准代码块语法（`` ``` ``），由 rehype-highlight 自动处理。

---

## 五、写作流程

### 通用流程

```
确定主题
  → 收集资料（task/webfetch），核查关键信息
  → 确定切入点（从哪个场景/人物/问题切入）
  → 写稿
  → npm run build 确认通过
  → commit & push 自动部署
```

### 注意事项

- **事实核查**：不要编造，对不确定的信息做 web 搜索验证
- **段落节奏**：不以机器判断断句。紧张处短段落，铺陈处长段落，让文字自然流淌
- **先框架后填充**：写前先搭好结构（小标题），再逐段填充内容

---

## 六、具体栏目索引

| 栏目名 | 策略文件 | 状态 |
|--------|----------|------|
| 深度学习的故事 | `DEEP-LEARNING-COLUMN-STRATEGY.md` | 进行中 |
| 统计学的故事 | `STATISTICS-COLUMN-STRATEGY.md` | 计划中 |
| 中国移动互联网的故事 | `CHINA-MOBILE-INTERNET-STRATEGY.md` | 计划中 |
| 代码的故事 | `CODE-COLUMN-STRATEGY.md` | 计划中 |
| 中国金融的故事 | `CHINA-FINANCE-COLUMN-STRATEGY.md` | 计划中 |
