# 《深度学习的故事》栏目策略

> 最后更新：2026-05-24
>
> 栏目从零重启。本文档记录定位、双线结构、写作方式与风格规范。

---

## 一、栏目定位

两条独立的内容线：

**历史线**——第三人称叙事，用杂志特写或叙事播客稿的风格讲深度学习发展史。纯故事，不深入论文细节。

**论文线**——独立的技术笔记，每篇聚焦一篇论文/一个算法，跟着学习进度写，在 WSL 中用 PyTorch/transformers/scikit-learn 跑代码验证。

两条线不绑定。读历史不需要懂代码，看论文笔记不需要知道上下文。

---

## 二、历史线

### 叙事形式

第三人称叙事，有场景、有人物、有节奏。不做对话体。

纯故事驱动，不贴代码、不铺公式。关键概念一句话解释即可，深入留在论文线。

### 段落风格（重要）

不以机器判断断句。段落长度由叙事节奏自然决定：

- 紧张处、快节奏处 → 短段落（3-4 句）
- 背景铺陈、历史脉络 → 长段落（6-10 句）
- 不带机械分段感，让文字自然流淌

一句话原则：**像人在写文章，不是 AI 在列要点。**

### 小标题

使用 `##` 作为段落间的小标题分隔。标题直接点明该段主题，不绕弯。

### 字数

单篇不限字数，以把故事讲完整为准。正常一篇 2000–4000 字均可，可更长。

### 图片

- 历史照片放在 `public/images/` 目录下
- markdown 中使用 `![](/images/文件名.jpg)` 引用
- 文末设「人物照片」章节，附本篇出场人物的肖像与简介
- 人物肖像姓名后用 `<strong>` 而非 `**` 加粗（因 `<img>` 后的 markdown 语法会被解析器忽略）

### 图片选材原则

除了人物肖像外，优先寻找：

1. **实验室/团队场景**——贝尔实验室的走廊、多伦多大学的办公室、UCSD 的研讨会。人在真实环境中的照片比证件照有温度得多
2. **历史性瞬间的纪实**——Hinton 在 NIPS 演讲的照片、LeCun 在贝尔实验室调试机器的画面、Krizhevsky 的卧室里的三块显卡
3. **当时的设备/环境**——Mark I 感知机本体、AlexNet 跑在 GTX 580 上的 setup、Rosenblatt 站在感知机前的合影

这些照片通常藏在大学档案馆、会议历史相册、人物传记配图中。

### 四阶段

| 阶段 | 时间跨度 | 内容 | 主线 |
|---|---|---|---|
| **序章：萌芽** | 1958–1980s | 感知机诞生 → AI 寒冬 → 反向传播 | 神经网络的第一次兴起与沉寂 |
| **奠基：CNN + RNN** | 1980s–2000s | LeNet → LSTM → DBN | 两个重要架构如何从边缘走到中心 |
| **爆发：ImageNet 时代** | 2006–2015 | AlexNet → ResNet → GAN | 大数据 + GPU 推动深度学习全面崛起 |
| **当代：Transformer 与大模型** | 2014–至今 | Attention → Transformer → GPT → 多模态 | 注意力机制改变一切 |

---

## 三、论文线

### 形式

独立技术笔记，每篇讲一个论文节点。

- 读论文 → WSL 跑 PyTorch 代码验证 → 写笔记
- 可长可短，取决于该论文的复杂度
- 包含真实代码、关键公式（`$$` / `$` 定界符）、架构图
- 与历史线独立，不假设读者读过任何历史篇

### 说明块规范

关键概念使用 `> **说明 · 概念名**` 格式。

### 节点列表

| 节点 | 核心论文 | 所属阶段 |
|---|---|---|
| 感知机 | Rosenblatt 1958 | 序章 |
| 反向传播 | Rumelhart 1986 | 序章 |
| CNN/LeNet | LeCun 1998 | 奠基 |
| RNN/LSTM | Hochreater 1997 | 奠基 |
| DBN | Hinton 2006 | 奠基 |
| AlexNet | Krizhevski 2012 | 爆发 |
| ResNet | He et al. 2015 | 爆发 |
| GAN | Goodfellow 2014 | 爆发 |
| 注意力 | Bahdanau 2014 | 当代 |
| Transformer | Vaswani 2017 | 当代 |
| BERT / GPT | Devlin 2018 / Radford 2018 | 当代 |
| Scaling Laws | Kaplan 2020 | 当代 |
| RLHF | InstructGPT 2022 | 当代 |
| 多模态 | CLIP / DALL-E | 当代 |

写作顺序可跳，跟着学习兴趣走。

---

## 四、跨页导航 & 排序

### 排序方式

同一 column 内的文章通过 frontmatter 的 `order` 字段控制顺序（升序）。不设 `order` 的文章按 `date` 排，排在所有设了 `order` 的文章之后。

### 导航

依赖 React 组件（`Post.tsx`）按上述排序自动生成上一篇/下一篇。

---

## 五、命名约定

文件名不加栏目名，直接用内容关键词。

### 历史线

```
感知机的诞生.md          # 序章·感知机
第一次AI寒冬.md          # 序章·AI 寒冬
反向传播的诞生.md        # 序章·BP
```

### 论文线

```
感知机源码解读.md        # 论文笔记·感知机
从零实现反向传播.md      # 论文笔记·BP
```

### 标题

每一章直接用主题当标题，不加"深度学习的故事"前缀。

---

## 六、写作流程

### 历史线

```
确定阶段/主题
  → 收集史实（task/webfetch），核查所有人名、日期、数据
  → 确定叙事切入点（从哪个场景/人物切入）
  → 写稿（自然段落，小标题分段，文末附人物照片）
  → npm run build 确认通过
  → commit & push 自动部署
```

### 论文线

```
选择论文
  → 读原文
  → WSL 跑源码验证
  → 写技术笔记（含代码、公式、架构图）
  → npm run build
  → commit & push
```
